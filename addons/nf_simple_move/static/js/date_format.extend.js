openerp.nf_simple_move.date_extend = function(instance){
	
/*	var old_fct=instance.web.parse_value;
	instance.web.parse_value = function (value, descriptor, value_if_empty) {
		return old_fct(value,descriptor,value_if_empty);
	}
*/
	
	
}